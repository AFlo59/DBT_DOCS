# =============================================================================
# Variables d'environnement - Template
# =============================================================================
# 
# INSTRUCTIONS:
# 1. Renommer ce fichier vers .env : rename env.example.txt .env
# 2. Remplir les valeurs avec vos credentials réels
# 3. Ne JAMAIS committer le fichier .env !
#
# =============================================================================

# -----------------------------------------------------------------------------
# Snowflake Configuration
# -----------------------------------------------------------------------------

# Compte Snowflake (ex: xy12345.eu-west-1)
DBT_SNOWFLAKE_ACCOUNT=your_account

# Utilisateur Snowflake
DBT_SNOWFLAKE_USER=your_username

# Mot de passe Snowflake
DBT_SNOWFLAKE_PASSWORD=your_password

# Rôle Snowflake (ex: TRANSFORMER, ANALYST)
DBT_SNOWFLAKE_ROLE=TRANSFORMER

# Warehouse Snowflake (ex: TRANSFORMING, COMPUTE_WH)
DBT_SNOWFLAKE_WAREHOUSE=TRANSFORMING

# Base de données Snowflake
DBT_SNOWFLAKE_DATABASE=ANALYTICS

# Schéma par défaut
DBT_SNOWFLAKE_SCHEMA=DBT_DEV

# -----------------------------------------------------------------------------
# Configuration DBT
# -----------------------------------------------------------------------------

# Environnement cible (dev, staging, prod)
DBT_TARGET=dev

# Nombre de threads pour le parallélisme
DBT_THREADS=4

# -----------------------------------------------------------------------------
# Authentification alternative (si key pair)
# -----------------------------------------------------------------------------

# Chemin vers la clé privée
# DBT_SNOWFLAKE_PRIVATE_KEY_PATH=/path/to/rsa_key.p8

# Passphrase de la clé privée (si chiffrée)
# DBT_SNOWFLAKE_PRIVATE_KEY_PASSPHRASE=your_passphrase

# -----------------------------------------------------------------------------
# BigQuery Configuration (si applicable)
# -----------------------------------------------------------------------------

# Chemin vers le fichier de credentials JSON
# DBT_BIGQUERY_KEYFILE=/path/to/service_account.json

# Projet GCP
# DBT_BIGQUERY_PROJECT=your-gcp-project

# Dataset
# DBT_BIGQUERY_DATASET=your_dataset

# -----------------------------------------------------------------------------
# CI/CD
# -----------------------------------------------------------------------------

# Token pour les APIs (GitHub, Slack, etc.)
# GITHUB_TOKEN=ghp_xxxxxxxxxxxx
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx

# =============================================================================
# Fin du fichier
# =============================================================================

